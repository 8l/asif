%load "Prelude";
%load "Repr";
%load "Size";

\f : Nat -> Nat -> Nat.
f (succ (succ (succ (succ (succ zero)))))
  (size
    [(Pi A:*. A -> A) -> (Pi A:*. A -> A)]
    [\x:(Pi A:*. A -> A). x (Pi A:*. A -> A) x])